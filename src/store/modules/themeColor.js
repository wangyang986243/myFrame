const themeColor  = {
    state:{
        themeColor:""
    },
    mutations:{
        SET_THEMECOLOR:(state,themeColor)=>{
            state.themeColor = themeColor
        }
    }
}

export default themeColor